on: [push]

jobs:
  # pass-tests:
  #   runs-on: ubuntu-latest
  #   name: Run tests successfully
  #   steps:
  #     # To use this repository's private action,
  #     # you must check out the repository
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Run Doc Detective
  #       uses: ./ # Uses an action in the root directory
  #       id: dd
  #       with:
  #         command: 'runTests'
  #         config: './artifacts/.doc-detective.json'
  #         input: './artifacts/doc-content-inline-tests.md'
          
  #     - name: Output results
  #       run: echo ${{ steps.dd.outputs.results }}

  # fail-and-exit:
  #   runs-on: ubuntu-latest
  #   name: Run tests, exit on fail
  #   steps:
  #     # To use this repository's private action,
  #     # you must check out the repository
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Run Doc Detective
  #       uses: ./ # Uses an action in the root directory
  #       continue-on-error: true
  #       id: dd
  #       with:
  #         command: 'runTests'
  #         config: './artifacts/.doc-detective.json'
  #         input: './artifacts/doc-content-inline-tests-fail.md'
  #         exitOnFail: true
          
  #     - name: Output results
  #       run: echo ${{ steps.dd.outputs.results }}

  create_issue:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Create issue using REST API
        run: |
          curl --request POST \
          --url https://api.github.com/repos/${{ github.repository }}/issues \
          --header 'authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' \
          --header 'content-type: application/json' \
          --data '{
            "title": "Automated issue for commit: ${{ github.sha }}",
            "body": "This issue was automatically created by the GitHub Action workflow **${{ github.workflow }}**. \n\n The commit hash was: _${{ github.sha }}_."
            }' \
          --fail
  # fail-and-create-issue: 
  #   runs-on: ubuntu-latest
  #   name: Run tests, create issue on fail
  #   steps:
  #     # To use this repository's private action,
  #     # you must check out the repository
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Run Doc Detective
  #       uses: ./ # Uses an action in the root directory
  #       id: dd
  #       with:
  #         command: 'runTests'
  #         config: './artifacts/.doc-detective.json'
  #         input: './artifacts/doc-content-inline-tests-fail.md'
  #         createIssueOnFail: true
          
  #     - name: Output results
  #       run: echo ${{ steps.dd.outputs.issueUrl }}

  #     - name: Delete issue
  #       run: gh issue delete $ISSUE
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         ISSUE: ${{ steps.dd.outputs.issueUrl }}
